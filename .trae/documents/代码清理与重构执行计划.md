## 详细执行计划

### 阶段一：清理未使用字段（低风险）

#### Task 1: 清理 engine.rs
- **文件**: `src/workflow/engine.rs` 第79行
- **操作**: 移除 `#[allow(dead_code)] max_parallel_workflows: usize`
- **验证**: 编译检查确保无其他引用

#### Task 2: 清理 execution_manager.rs
- **文件**: `src/workflow/execution_manager.rs` 
- **操作**: 读取完整文件，识别并移除未使用字段
- **验证**: 编译检查

#### Task 3: 清理 app.rs
- **文件**: `src/interfaces/tui/app.rs` 第46-49行, 第70-71行
- **操作**: 移除 `#[allow(dead_code)] event_handler` 和 `action_dispatcher`
- **验证**: 编译检查

### 阶段二：统一错误处理（中风险）

#### Task 4: 创建 error/common.rs
创建通用错误变体模块：
```rust
pub enum CommonError {
    Io(std::io::Error),
    Validation { message: String },
    NotFound { resource: String },
    Timeout { duration: Duration },
}
```

#### Task 5: 重构 error.rs
- 使用 `CommonError` 替代重复定义
- 保持向后兼容

#### Task 6: 重构 plugins/error.rs
- 使用 `CommonError` 作为基础
- 添加插件特定上下文

#### Task 7: 重构 interfaces/cli/error.rs
- 使用 `CommonError` 作为基础
- 添加 CLI 特定上下文

#### Task 8: 重构 interfaces/tui/error.rs
- 使用 `CommonError` 作为基础
- 添加 TUI 特定上下文

### 阶段三：提取插件通用结构（高风险）

#### Task 9: 创建 plugins/common.rs
定义通用插件结构：
```rust
pub struct PluginBase<E, R> {
    info: PluginInfo,
    status: PluginStatus,
    config: Option<PluginConfig>,
    runtime_config: R,
    environment: Arc<Mutex<E>>,
    tools: Vec<Arc<dyn ToolNode>>,
}
```

#### Task 10: 重构 PythonPlugin
- 使用 `PluginBase` 替代重复字段
- 保持所有功能不变

#### Task 11: 重构 NodeJsPlugin
- 使用 `PluginBase` 替代重复字段
- 保持所有功能不变

#### Task 12: 重构 DockerPlugin
- 使用 `PluginBase` 替代重复字段
- 保持所有功能不变

### 验证步骤

每个 Task 后执行：
1. `cargo check` - 编译检查
2. `cargo clippy` - 代码质量检查
3. `cargo test` - 运行测试

### 回滚策略

- 每个 Task 独立提交
- 发现问题立即回滚
- 保持主分支可编译状态

### 预期收益

- 减少重复代码 ~1000 行
- 提高可维护性
- 统一错误处理模式

请确认此计划后开始执行。
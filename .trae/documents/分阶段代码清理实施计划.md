## 分阶段代码清理实施计划

### 阶段1: 清理未使用字段（低风险）

#### Step 1.1: 清理 engine.rs
**文件**: `src/workflow/engine.rs`
**操作**:
- 移除第 78-80 行的 `#[allow(dead_code)] max_parallel_workflows: usize`
- 更新构造函数中的初始化（第 138 行）
- 验证编译: `cargo check`

#### Step 1.2: 清理 execution_manager.rs
**文件**: `src/workflow/execution_manager.rs`
**操作**:
- 移除第 111-116 行的 `queue_notify` 和 `background_tasks` 字段
- 移除未使用的导入 `Notify` 和 `JoinHandle`
- 更新构造函数中的初始化
- 验证编译: `cargo check`

#### Step 1.3: 清理 app.rs
**文件**: `src/interfaces/tui/app.rs`
**操作**:
- 移除第 46-49 行的 `event_handler` 和 `action_dispatcher` 字段
- 移除第 70-71 行的 `last_update` 字段
- 移除未使用的导入 `EventHandler`, `ActionDispatcher`
- 更新构造函数中的初始化
- 验证编译: `cargo check`

### 阶段2: 统一错误处理（中风险）

#### Step 2.1: 创建 error/common.rs
**文件**: `src/error/common.rs` (新建)
**内容**:
```rust
//! Common error types shared across modules
use thiserror::Error;

#[derive(Error, Debug, Clone)]
pub enum CommonError {
    #[error("IO error: {message}")]
    Io { message: String, path: Option<String> },
    #[error("Validation error: {message}")]
    Validation { context: String, message: String },
    #[error("Not found: {resource}")]
    NotFound { resource_type: String, resource: String },
    #[error("Timeout after {duration:?}")]
    Timeout { operation: String, duration: std::time::Duration },
}
```

#### Step 2.2: 在 WorkflowError 中集成 CommonError
**文件**: `src/error.rs`
**操作**:
- 添加 `pub mod common;`
- 在 `WorkflowError` 中添加 `Common(#[from] CommonError)` 变体
- 保持所有现有错误变体不变（向后兼容）
- 验证编译: `cargo check`

### 阶段3: 插件通用结构（高风险，可选）

#### Step 3.1: 创建 plugins/common.rs
定义通用插件基础结构，供 Python/Node.js/Docker 插件复用

### 验证步骤

每个阶段完成后执行:
1. `cargo check` - 编译检查
2. `cargo clippy` - 代码质量检查
3. `cargo test` - 运行测试

### 回滚策略

- 每个 Step 独立提交
- 使用 `git checkout <file>` 可快速回滚
- 保持主分支可编译状态

请确认后开始执行阶段1。
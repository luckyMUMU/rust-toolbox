## 执行计划

### 阶段一：代码清理（预计 2-3 小时）

#### Task 1.1: 删除未使用模块
- [ ] 删除 `src/tools/composable.rs`（整个模块 DEPRECATED）
- [ ] 删除 `src/tools/compat.rs`（兼容性代码）
- [ ] 删除 `src/plugins/wasm.rs`（模块被注释）
- [ ] 更新相关 `mod.rs` 文件
- [ ] 运行 `cargo check` 验证编译通过

#### Task 1.2: 清理未使用字段和导入
- [ ] 清理 `src/workflow/engine.rs` 中的 `#[allow(dead_code)]` 字段
- [ ] 清理 `src/workflow/execution_manager.rs` 中的未使用字段
- [ ] 清理 `src/interfaces/tui/app.rs` 中的未使用字段
- [ ] 运行 `cargo clippy` 检查未使用代码
- [ ] 运行 `cargo test` 确保测试通过

### 阶段二：代码重构（预计 4-6 小时）

#### Task 2.1: 统一错误处理
- [ ] 创建 `src/error/common.rs` 模块
- [ ] 提取通用错误变体（Io, Validation, NotFound, Timeout）
- [ ] 重构 `src/error.rs` 使用新的通用错误
- [ ] 重构 `src/plugins/error.rs`
- [ ] 重构 `src/interfaces/cli/error.rs`
- [ ] 重构 `src/interfaces/tui/error.rs`
- [ ] 运行测试验证错误处理正常

#### Task 2.2: 提取插件通用结构
- [ ] 创建 `src/plugins/common.rs` 模块
- [ ] 定义 `PluginBase<E, R>` 泛型结构体
- [ ] 重构 `PythonPlugin` 使用 `PluginBase`
- [ ] 重构 `NodeJsPlugin` 使用 `PluginBase`
- [ ] 重构 `DockerPlugin` 使用 `PluginBase`
- [ ] 创建通用 `PluginBuilder`
- [ ] 运行测试验证插件功能正常

#### Task 2.3: 创建外部进程执行器
- [ ] 创建 `src/plugins/process_executor.rs`
- [ ] 提取 Python/Node.js 的共同执行逻辑
- [ ] 重构 `python.rs` 使用新的执行器
- [ ] 重构 `nodejs.rs` 使用新的执行器
- [ ] 运行测试验证进程执行正常

### 阶段三：文档重构（预计 3-4 小时）

#### Task 3.1: 创建 L2 逻辑流转层
- [ ] 创建 `docs/02_logical_workflow/` 目录
- [ ] 编写 `workflow_execution.pseudo` - 工作流执行伪代码
- [ ] 编写 `plugin_loading.pseudo` - 插件加载伪代码
- [ ] 编写 `tool_execution.pseudo` - 工具执行伪代码
- [ ] 编写 `error_handling.pseudo` - 错误处理伪代码

#### Task 3.2: 整理 L3 技术规格层
- [ ] 创建 `docs/03_technical_spec/` 目录
- [ ] 从 `SPEC.md` 提取接口契约到 `interfaces.md`
- [ ] 创建 `data_models.md` 数据模型定义
- [ ] 创建 `error_codes.md` 错误代码定义

#### Task 3.3: 整理 L4 决策参考层
- [ ] 创建 `docs/04_context_reference/` 目录
- [ ] 从 `PRODUCT_DESIGN.md` 提取 ADR 到 `architecture_decision.md`
- [ ] 创建 `performance_benchmarks.md`
- [ ] 创建 `migration_notes.md`

#### Task 3.4: 清理和归档
- [ ] 将过时文档移动到 `docs/archive/`
- [ ] 删除重复的 AGENTS.md 文件
- [ ] 更新 `docs/INDEX.md` 反映新结构
- [ ] 更新 `README.md` 精简内容

### 阶段四：版本关联机制（预计 1 小时）

#### Task 4.1: 添加版本标记
- [ ] 在关键文档中添加版本标记
- [ ] 创建 `docs/VERSION_MAPPING.md` 版本映射表
- [ ] 更新 `Cargo.toml` 中的文档引用

#### Task 4.2: 最终验证
- [ ] 运行 `cargo build` 确保编译通过
- [ ] 运行 `cargo test` 确保所有测试通过
- [ ] 运行 `cargo clippy` 确保无警告
- [ ] 验证文档链接有效性

## 预期收益

| 指标 | 优化前 | 优化后 | 改善 |
|------|--------|--------|------|
| 代码行数 | ~27000 行 | ~24000 行 | -11% |
| 重复代码 | ~2700 行 | ~500 行 | -81% |
| 未使用代码 | ~1000 行 | 0 行 | -100% |
| 文档结构 | 混乱 | 分级清晰 | 结构化 |

## 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 删除代码导致功能损坏 | 高 | 每阶段后运行完整测试套件 |
| 重构引入新 bug | 中 | 小步提交，频繁验证 |
| 文档链接失效 | 低 | 使用相对路径，批量检查 |

## 执行顺序建议

1. **先做代码清理**（Task 1.1, 1.2）- 风险最低，立即见效
2. **再做文档重构**（Task 3.x）- 不影响代码功能
3. **最后代码重构**（Task 2.x）- 需要更多测试验证
4. **版本关联收尾**（Task 4.x）- 最后统一标记

请确认此计划后，我将开始执行第一阶段任务。